import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2023-recreio-da-juventude',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
